sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t nickeick/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nickeick/docker-react npm run test
